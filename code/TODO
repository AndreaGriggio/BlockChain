Da fare :
1. [ ] main.c: creazione+bind+listen MINERS_SOCKET prima del fork 
    (lato server client->miner; ORA non lo ascolta nessuno). 
    NB: le FIFO le creano da soli node e miner

2. [ ] main.c: fork/exec dei figli (via MinerLauncher/ClientsLauncher + node), tutti in un process group

3. [ ] main.c: 
    REPL (submit, request, save, pause, resume, stop)
    . pause/resume/stop via killpg (SIGSTOP/SIGCONT/SIGTERM); 
    request/save leggono il CSV condiviso

4. [ ] main.c: shutdown pulito (killpg + waitpid, sem_unlink, unlink socket/fifo)

7. [ ] report.pdf (10% voto)

8. [ ] integrazione end-to-end + edge case 2.2.7 (crash, CSV corrotto, pause/resume)

9.[ ] minerCommunicationProcess.c: 
    main loop e' un while vuoto. 
    Serve: accept() client sul socket,
    creazione blocco reale,
    rilettura tip dal CSV, 
    sleep interrompibile, 
    reazione a notify VALID/INVALID. 
    srand mancante

10. [ ] NodeLauncher (analogo a MinerLauncher) 
